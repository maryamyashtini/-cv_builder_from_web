name: cv_builder_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - openai
      - selenium
      - webdriver-manager
      - beautifulsoup4
      - requests
      - python-dotenv
      - ipython
      - notebook

